let numeros = [1,2,3,4,5,10,20,32,40,1002,1320,1326];

function multiplicação(array){
    let resultado = array.map(numero => numero * 5);
    console.log()
    
    resultado.forEach(resultado => {
        console.log(resultado);
        
    });
    
    
}

multiplicação(numeros);
